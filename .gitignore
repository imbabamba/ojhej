node_modules/
package-lock.json

# Secrets — never committed. These live as Bunny Edge Scripting secrets.
.env
.env.*
!.env.example

# Generated print output — regenerate with `deno task print-pack`
dist/

# Deno
.deno/

# OS
Thumbs.db
.DS_Store

# Local dev only: filesystem store and captured mail
.devdata/
.devmail/
.devserver.log

# print packs are generated on demand, never committed
tryck/

# build output, regenerated by deno task bundle
dist/
tryck-ci/
devserver.log

# Playwright driving artefacts, written where the browser was pointed
.playwright-mcp/

# Kept privately: the decision log, the research, the deploy runbook and the approved screen
# designs. Comments in the source cite them as "status.md" and "mockups/…"; the reasoning is
# always in the comment itself, so the citation is a footnote rather than a missing dependency.
specs/
mockups/

# Local agent tooling, nothing to do with the service
.claude/
